#recieve input from user
name=input('What is your name?')
# this msg will printed on terminal and it will wait for the user to enter value, whatever we enter will be returned by this input function
# so we'll store it in a variable
print('Hi! '+name)
